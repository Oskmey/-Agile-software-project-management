using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TrashItem : Item
{
    // Start is called before the first frame update
    void Start()
    {
        GetComponent<TrashScript>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
